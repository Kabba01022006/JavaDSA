package _10_strings;

public class _08_stringToInteger {
    public static void main(String[] args) {
        //string to integer can omly be converted to integer when string only has numbers in it
        String a = "4545";
        int convtd = Integer.parseInt(a);
        System.out.println(convtd);
    }
}
